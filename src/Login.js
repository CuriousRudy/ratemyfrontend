import React from 'react';

class Login extends React.Component {
  render() {
    return (
      <div className="container">
        <h4>this could be the area that has a form for logging in</h4>
      </div>
    );
  }
}

export default Login;
